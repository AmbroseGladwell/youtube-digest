{
  "name": "@overview/api",
  "version": "0.0.0",
  "private": true,
  "description": "The sync API: Fastify on Postgres, the server half of the sync engine (docs/architecture/api.md, docs/features/sync-api.md). @overview is a placeholder scope — product naming is parked (docs/architecture/v1-architecture-decisions.md).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "start": "node dist/server.js",
    "dev": "npm run build && node --env-file=../../.env dist/server.js",
    "mint-session": "npm run build && node --env-file=../../.env dist/scripts/mintSession.js"
  },
  "dependencies": {
    "@overview/domain": "*",
    "fastify": "^5.12.5",
    "fastify-plugin": "^6.0.0",
    "pg": "^8.23.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@overview/store-conformance": "*",
    "@types/node": "^22.7.5",
    "@types/pg": "^8.23.1",
    "typescript": "^5.6.3"
  }
}
