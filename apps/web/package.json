{
  "name": "@overview/web",
  "version": "0.0.0",
  "private": true,
  "description": "The web app: a thin static-hosted SPA shell that mounts @overview/app-core, backed by the free tier's local IndexedDB store (docs/architecture/v1-architecture-decisions.md). @overview is a placeholder scope — product naming is parked.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@overview/app-core": "*",
    "@overview/store-local": "*",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "sass": "^1.104.1",
    "typescript": "^5.6.3",
    "vite": "^8.3.0"
  }
}
