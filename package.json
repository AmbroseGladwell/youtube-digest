{
  "name": "video-digest",
  "private": true,
  "description": "Monorepo root. Product name is parked (docs/v1-architecture-decisions.md) — this is a placeholder, not a decision.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
