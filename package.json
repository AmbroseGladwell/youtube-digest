{
  "name": "video-digest",
  "private": true,
  "type": "module",
  "description": "Monorepo root. Product name is parked (docs/architecture/v1-architecture-decisions.md) — this is a placeholder, not a decision.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:packages": "npm run build --workspace packages/types --workspace packages/transcripts --workspace packages/generation --workspace packages/store-conformance --workspace packages/store-local --workspace packages/app-core",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
