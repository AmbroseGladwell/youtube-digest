{
  "name": "@overview/store-conformance",
  "version": "0.0.0",
  "description": "Behavioural conformance suites for OverviewStore/SettingsStore — run against any backend (in-memory, local, API) to prove it honours the interface contract. @overview is a placeholder scope — product naming is parked (docs/architecture/v1-architecture-decisions.md).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/"
  },
  "dependencies": {
    "@overview/types": "*"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}
