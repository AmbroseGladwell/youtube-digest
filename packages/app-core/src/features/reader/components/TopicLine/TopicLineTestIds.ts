export const topicLineTestIds = {
  root: "TopicLine.root",
  chip: "TopicLine.chip",
  removeChip: (name: string) => `TopicLine.removeChip.${name}`,
  addButton: "TopicLine.addButton",
  scrim: "TopicLine.scrim",
};
