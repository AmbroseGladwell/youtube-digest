// The ready state stands down on its own — the finished note is in the library by then,
// with the design's entrance marking it (docs/features/overview-redesign.md). A failure
// never does: it has to be read.
export const READY_DISMISS_MS = 8_000;
